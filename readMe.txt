
Assuming the current directory is the Hangman folder (can see class, src, .jar, etc)

to compile: javac @options @sources
to run: java -cp class Hangman

jar: java -jar Hangman.jar	OR	doubleclick .jar if system is setup for that
